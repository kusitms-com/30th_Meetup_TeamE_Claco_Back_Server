spring:
  application:
    name: claco
  profiles:
    active:
      - prod
    group:
      local:
        - local
      prod:
        - prod
    include:
      - oauth
      - aws
  servlet:
    multipart:
      resolve-lazily: true
      max-file-size: 10MB
      max-request-size: 10MB
